extends layout

block content
  h2 Top Teams
  ul.team-list
    each team in teams
      li
        a(href=`/team/${team.team.id}`)
          img(src=team.team.logo, alt=team.team.name)
          span= team.team.name